/**
 * @type {import('tailwindcss').Config}
 * NOTE: This file is not actively used.
 * Tailwind CSS is loaded via a CDN script in `index.html`.
 * This file exists to prevent potential build errors in environments
 * that might look for a Tailwind configuration file.
 */
const config = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
